# 更新记录 / Changelog

## [0.3.0] - 2026-08-26

### 破坏性变更 / Breaking Changes

- **网络协议版本升级到 4**：子弹增量位置改用双精度传输，子弹属性快照改为随子弹独立下发（内容寻址只保留纯视觉），子弹条目新增第三方扩展字段；0.3.0 与旧版客户端/服务端不互通，联机需两端同时升级。
- **查询返回类型统一**：`getGunId`、`getState` 改为返回 `Optional`（与其余查询一致），不再返回 `null`。
- **插件安装前事件增强**：携带框架选中的槽位种类，并支持监听器附带自定义取消原因（构造参数变化）。

### 新增 / Added

- **插件支持 Java 注册与动态提供者**：与枪械侧对称，可程序化注册插件定义（随机战利品、动态词缀等玩法不再是死路）。
- **子弹同步扩展通道**：第三方可给每颗子弹附带自定义字节数据并在客户端渲染对象上读取（自定义转速/朝向等字段的同步不再需要自建网络）。
- **子弹同步可调优**：距离分档、分档更新频率与全量同步间隔移入 `modularshoot-common.toml`，服务器可按需调节带宽。
- **安装新增“插件定义缺失”专用错误**：数据包重载后条目消失时不再误报“没有槽位”。
- **门面新增扩展点清单文档**：按子系统列出全部注册/事件/钩子入口，第三方可一站式查阅。

### 变更 / Changed

- **特性钩子只对携带该特性的子弹派发**：消除“注册即对所有子弹生效、钩子自查”的误导语义，同时降低高弹量下的派发开销。
- **射击谓词/效果注册幂等**：重复注册同一实例不再叠加。
- **描边变色注册支持任意线程调用**：纹理释放自动延迟到渲染线程，不再可能跨线程操作 GPU。
- **批量卸载事件文档修正**：明确批量卸载时属性刷新延迟到批次末尾。
- **绑定索引重注册修复式重建**：换绑不同物品时旧索引自动清理。
- **门面类级注释修正**：如实描述其覆盖全部子系统（原文误称仅插件）。
- **两套重复的物品绑定索引收敛为单一泛型实现**。
- **渲染纹理缓存键跨帧复用**：不再每帧重建缓存键并重算散列。

### 修复 / Fixed

- **修复弹药伤害类型预设永远失效**：经状态系统写入的预设现在能被射击管线正确读取。
- **修复损坏/恶意子弹同步包可触发巨量内存分配**：条目数量与扩展字段长度解码时设防。
- **修复远坐标子弹位置精度**：增量同步位置不再降为单精度，消除 |16M| 以外坐标漂移与首帧跳变。
- **修复状态清除后客户端残留陈旧值**：清除现在同样触发节流同步。
- **修复损坏/改型的状态数据击穿 tooltip 与渲染读取**：解码失败降级为零值并限流告警。
- **修复状态默认值类型漂移**：`value_type: float` 的 JSON 默认值不再被解成 double 导致读取归零。
- **修复数据包容错文档误导**：如实说明单条 JSON 解析失败会中止整表加载（原版管线行为）。

### 测试 / Testing

- 全量 **694** 个测试通过（新增 21 条：同步扩展通道、插件 Java 注册、伤害预设读取、解码防御、绑定索引修复、默认值收敛、钩子过滤）。

## [0.2.0] - 2026-08-14

### 破坏性变更 / Breaking Changes

- **移除 5 个已废弃的 `ModularShootAPI` 重载**：显式 `RegistryAccess` 版本的卸载系列与不刷新属性的 `setPluginLocked` 已删除；请改用无 `RegistryAccess` 的推荐重载，或直接调用 `PluginUninstallService` / `PluginLockService`。
- **移除 `BulletManager#fireBullet`**：独立发弹请改用 `ModularShootAPI.fireBullet` 或新的 `CreationCoordinator`。
- **网络协议版本升级到 3**：0.2.0 与旧版客户端/服务端不互通，联机需两端同时升级。

### 性能 / Performance

- **注册表查询全面缓存**：热路径不再重复解析枪械/插件/状态等定义，射击、安装卸载与 tooltip 明显变快。
- **子弹同步大幅减负**：新增空间索引、位置量化与距离降频，并压缩全量/增量同步，多人高弹量场景带宽与 CPU 显著下降。
- **状态系统提速**：同值写入短路、快速 NBT 编解码、PlayerState 节流，高频状态累积不再产生无谓拷贝与同步。
- **渲染与 Tooltip 减帧**：Tooltip 帧级缓存、反向索引、纹理键哈希缓存、描边懒生成、渲染对象复用，降低每帧开销。
- **Reload 减卡顿**：共享注册表快照与修饰符缓存，大型数据包 reload 不再重复全表解析。
- **广播聚合**：命中与射击动画按 tick 合并，减少包数量与全玩家扫描。

### 架构 / Architecture

- **公共 API 单向化**：物品识别下沉内部工具，内部服务不再反向依赖门面。
- **客户端/服务端边界更清晰**：客户端网络处理移入 client 包，common 不再引用 Minecraft 客户端类。
- **职责拆分**：子弹管理器拆分为存储、创建、网络标记三层；射速公式抽为单一共享来源。

### 健壮性 / Robustness

- 无处理器短路、异常日志节流、网络包防御性解码、状态清除短路、缓存生命周期清理等多项加固。

### 测试 / Testing

- 全量 **673** 个测试通过。

## [0.1.7] - 2026-08-14

### 新增 / Added

- **安装 API 支持指定优先种类**：`installPlugin` 新增可选的优先种类参数（如插件面板把插件拖到某个种类安装区时指定该种类），提示命中时直接装入该种类，未命中自动回退原选择逻辑；旧调用方零改动、行为不变。
- **有效插槽查询门面**：新增 `getEffectiveSlots` API，可直接查询枪械当前各种类的有效容量（含已装插件的加槽效果），UI 渲染安装区不再需要自行拼装。

### 测试 / Testing

- 新增 14 条测试覆盖优先种类命中/未命中回退/满槽回退与全流程传导（含门面透传）；全量 452 条通过。

## [0.1.6] - 2026-08-13

### 性能优化 / Performance

- **物品识别与绑定查询大幅提速**：背包扫描、物品判断等高频路径不再每次重建查询表，多人服务器每 tick 开销显著下降。
- **状态写入去重**：写入与当前相同的值不再触发数据拷贝与同步，高频状态（热量累积等）不再产生无谓的网络流量。
- **子弹同步包更小**：增量同步条目体积减半，高弹量场景带宽明显下降。
- **霰弹/多弹丸开销降低**：变体池每发只构建一次，逐弹丸仅做独立随机；子弹视觉的注册表查询改为按实例缓存，高射速霰弹不再重复查表。
- **批量卸插件只重算一次属性**：一次卸多个插件时，属性重算从逐次执行改为整批一次。
- **纹理缓存更可靠**：动态纹理缓存改为按数值比较键值，插件重载或资源刷新后不会触发不必要的纹理重合成。
- **渲染开销削减**：子弹排序、快照读取等每帧/每 tick 的临时分配被消除。

### 修复 / Fixed

- **切换维度后子弹可能短暂消失**：切维度立即重置子弹同步状态，不再依赖数秒后的周期性全量同步兜底。
- **资源重载后插件视觉可能陈旧**：F3+T 刷新后同步清理叠加层缓存，保证画面与资源包一致。
- **绑定结果更确定**：同一物品被多个 Java API 绑定重复注册时，生效规则统一为字典序最小键（与数据包通道一致），此前依赖不确定的扫描顺序。
- **命中包编码修正**：非实体命中不再把 -1 哨兵编码为 varint（负数 varint 反而更大），改为标志位 + 条件 varint，实体与非实体命中都不再劣于原定长编码。
- **高速移动时射击音效提前消失**：射击音效与插件安装音效改为跟随玩家移动，高速移动中不再因离开音效触发位置而迅速听不到。

### 测试 / Testing

- 新增 18 条测试覆盖绑定索引、缓存键值语义、网络压缩精度、LRU 逐出策略、批量卸载单次重算等；全部 438 条通过。

## [0.1.5] - 2026-08-11

### 修复 / Fixed

- **专用服务器崩溃**：修复专用服务器上枪械/插件物品名称解析抛 `NoClassDefFoundError` 崩服的问题（`/modularshoot stats` 等命令、死亡消息等路径均受保护）。
- **插件裸键命名空间漂移**：插件定义的 `traits`/`adds_variants` 裸键与枪械一致归入 `modularshoot` 命名空间，枪械与插件同名的特性/变体现在可以正常合并。
- **零速子弹永不消亡**：速度为 0 的子弹不再永久驻留，立即按过期移除。
- **reload 校验误报**：插件标签仅部分未匹配时不再误报"装不上任何枪"；指向 Java API 注册枪械的合法绑定不再误报"未找到"。
- **第一人称后坐方向修正**：枪口下压修正为微微上抬（后收进屏幕 + 枪口朝观察者上抬）。

### 改进 / Improved

- **卸载超编预检测试**：0.1.4 的超编预检与随机卸载候选过滤补 10 条自动化测试。
- **健壮性**：非法伤害类型、损坏状态数据、槽位数值溢出、共享可变引用等边界情况不再导致崩溃或异常。
- **一致性**：卸载流程尊重事件监听器的修改；tooltip 插件栏计数与超编判定口径统一；登出清理节流表；reload 校验补绑定表与 shooters 汇总。
- **渲染**：3D 子弹光照改用插值位置，高速移动时更准确。
- **清理**：删除 8 处死代码，修正 7 处过时 javadoc。
- **测试补强**：出厂数据包 JSON 全量解码断言（45 个文件）；`BulletS2CPacket` 编解码往返测试；全量 414 用例。
- **本地化补强**：tooltip 计数括号与锁定锚字符改为 lang 键，en_us 下不再显示 CJK 标点。

## [0.1.4] - 2026-08-10

### 新增 / Added

- **动态枪械定义提供者**：`GunRegistry` 新增 `registerGunDefinitionProvider` 通道，`getGun` 查询顺序扩展为 Java API 注册 → 提供者 → 数据包注册表；支持 per-player / 运行时生成的动态定义（"玩家即枪"类玩法），框架内所有定义查询点零改动受益。门面 `ModularShootAPI.registerGunDefinitionProvider` 对外暴露；未注册提供者时行为完全一致。
- **插件定义支持 `adds_slots` 字段**：插件安装后可增加指定槽位数量，甚至创造枪械原本没有的槽位类型（键须为完整命名空间或 `modularshoot:` 裸键，值为任意整数，负数表示占用槽位）。有效容量 = 枪械 slots + 已装插件 adds_slots 聚合；安装匹配、tooltip 插件栏均按有效槽位计算。
- **卸载超编预检**：非 force 卸载若会导致任意槽位类型在拆卸后仍超编（不限于加槽插件本身——卸载后残留超编即拒绝，返回 `WOULD_OVERFLOW`）；force 卸载可绕过，超编插件滞留保留但该槽位不可再装；随机卸载自动过滤会触发超编的候选。
- **reload 校验**：`adds_slots` 键引用 `plugin_types` 注册表存在性检查（WARN）。

## [0.1.3] - 2026-08-08

### 新增 / Added

- **枪械定义支持 `extra_values` 字段**：`GunDefinition` 新增命名空间数值扩展字段（键须带完整命名空间，与插件侧一致）；`ModularShootAPI.getExtraValueSums` / `getExtraValue` 语义升级为"枪械定义基础值 + 已安装插件累计值"的一站式总和（枪械定义失效时基础值不参与，与插件降级口径一致）。示例：`blood_sword` 声明 `modularshoot:demo_rarity` 基础值 10。

### 修复 / Fixed

- **修复子弹飞行视觉回弹**：`BulletRenderDispatcher` 每帧统一读取一次时钟供全帧子弹共用（`TimeBasedInterpolationTest`）。

- **修复子弹方形纹理透明区域遮挡后方子弹**：billboard 渲染的 RenderType 写掩码由默认 `COLOR_DEPTH_WRITE` 改为 `COLOR_WRITE`（深度只读不写）。

## [0.1.2] - 2026-08-08

### 变更 / Changed

- **移除框架默认命中粒子**：`ClientHitEffectHandler` 不再生成任何默认粒子（伤害指示 `DAMAGE_INDICATOR` / 暴击 `CRIT` / 方块破坏），命中视觉特效完全交由 `ClientBulletHitEvent` 监听方自行实现；数据驱动命中音效（枪械 `sounds` 槽位）保留，取消事件仍可跳过默认音效。相关 javadoc（`ClientBulletHitEvent`、`ModularShootPayloads`）同步更新。

### 改进 / Improved

- 重绘子弹默认纹理（`textures/bullet/default.png`），观感大幅提升。

## [0.1.1] - 2026-08-08

### 新增 / Added

- **第一人称发射动画**：新增 `FirstPersonRecoilKick` 后坐曲线，开火瞬间枪身沿 +X 后收（上限 0.2 格）、枪口绕 +Z 上抬（上限 8°），随后快速回落；由 `shootAnimTimer` 驱动，与第三人称手臂后坐姿势、`per_shot` 射击纹理同节奏触发（`GunItemRenderer`、`FirstPersonRecoilKickTest`）。
- 射击谓词失败消息与 `StateDisplay.format` 支持 `lang:` 前缀键（本地化机制补强）。

### 改进 / Improved

- **全量本地化**：提示文本（tooltip 标题、快捷键提示、插件行/分隔符、降级显示等约 30 处）、5 个命令子命令反馈全部改为 lang 键（`modularshoot.command.*`）。
- 31 个示例数据包 JSON 的 `name` / `brief` / `description` / `display` 改为 `lang:` 键，`§` 颜色码移入 lang 值；`en_us` / `zh_cn` 各新增 95 键（55 → 150），示例内容英文全量翻译。
- `DatapackLoadSummary` / `DatapackReloadListener` / `ItemBindingValidator` 日志英文化。
